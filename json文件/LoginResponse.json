{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "userId": { "type": "string", "format": "uuid", "description": "用户唯一标识符" },
    "token": { "type": "string", "description": "认证令牌" }
  },
  "required": ["userId", "token"],
  "description": "登录响应"
}